# 実装方針

## MVPを最優先する

- ユーザーが明示した機能だけを実装する。便利そうな機能を推測で追加しない。
- 説明文、補足文、案内文、状態表示、設定サマリー、コピーボタンなどを、明示的な依頼なしに追加しない。
- UIには操作に必要な入力、ボタン、短いラベルだけを置く。
- 装飾、テーマ、アニメーション、アイコン、カード、影、色付けなどのデザインを、明示的な依頼なしに追加しない。
- CSSは余白、配置、操作可能なサイズなど、機能と最低限の操作性に必要な範囲に限定する。
- 既に簡素化または削除されたUIや文言を、実装上の都合で復活させない。参照先がない場合はコード側の依存を削除する。
- 要求外のリファクタリングや画面変更を同時に行わない。
- Unity画面、スマホ画面、接続サービスのすべてにこの方針を適用する。

判断に迷う追加要素は実装しない。必要性がある場合は、追加前にユーザーへ確認する。

## リポジトリの境界

- このディレクトリはプロジェクト全体の共通コンテキストを置く場所であり、Gitリポジトリではない。
- 個別のソフトウェアリポジトリは `apps/` 配下にまとめて配置する。
- 現行ハードウェアは `apps/HW`、Unityは `apps/unity`、接続サーバーとスマホ画面は `apps/phone` の独立リポジトリで管理する。
- 過去のリポジトリや参照用資料は `reference/` 配下に配置する。
- `apps/kojo_HW` は過去のハードウェア用リポジトリであり、明示的な依頼がない限り変更しない。
- 変更は依頼対象のリポジトリ内に限定する。通信形式など複数リポジトリにまたがる変更は、送信側と受信側の整合性を確認する。

## ルールとコンテキストの管理

- プロジェクト全体に共通する確定ルールは、この `AGENTS.md` に記録する。
- ハードウェア、Unity、サーバーだけに関係する確定ルールや前提は、それぞれのリポジトリ直下の `AGENTS.md` に記録する。
- 作業中に新しい設計判断、制約、実行手順が確定した場合は、その作業と同時に適切な `AGENTS.md` または既存ドキュメントへ追記する。
- 一時的な進捗、未確認の案、秘密情報、生成物の状態は `AGENTS.md` に記録しない。
- より深い階層の `AGENTS.md` は、その領域固有のルールとして共通ルールに追加して適用する。最新のユーザー指示が最優先である。
